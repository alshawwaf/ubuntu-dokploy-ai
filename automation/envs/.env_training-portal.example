# ============================================================================
# Training Portal - Environment Configuration
# ============================================================================
# Copy this file to .env_training-portal and fill in your values
# [REQUIRED] = Must be set for app to work
# [OPTIONAL] = Has defaults, can be omitted
# ============================================================================

# ──────────────── Docker Compose ────────────────
COMPOSE_PROJECT_NAME=training-portal

# ──────────────── Database [REQUIRED] ────────────────
DATABASE_URL=postgresql://admin:password@db:5432/training_portal
DB_USER=admin
DB_PASSWORD=your-secure-db-password
DB_NAME=training_portal

# ──────────────── Frontend [REQUIRED] ────────────────
FRONTEND_PORT=9090
FRONTEND_URL=https://training.example.com

# ──────────────── Superadmin [REQUIRED] ────────────────
# Initial admin user created on startup
SUPERADMIN_EMAIL=admin@{{DOMAIN}}
SUPERADMIN_PASSWORD=your-secure-admin-password

# ──────────────── vSphere Integration [OPTIONAL] ────────────────
# Configure via UI instead for production
VSPHERE_HOST=192.168.10.10
VSPHERE_USER=administrator@vsphere.local
VSPHERE_PASSWORD=your-vsphere-password
VSPHERE_PORT=443
# TLS verification on by default. Set to false ONLY for a self-signed lab cert.
VSPHERE_VERIFY_SSL=true

# ──────────────── Registration [OPTIONAL] ────────────────
# Comma-separated domains allowed to register
ALLOWED_DOMAINS=example.com,yourdomain.com

# ──────────────── Guacamole [REQUIRED for console access] ────────────────
GUACAMOLE_SECRET_KEY=your-random-guacamole-secret-key
GUACAMOLE_URL=http://guacamole:8080/guacamole
GUACAMOLE_EXTERNAL_URL=http://localhost:8085/guacamole

# ──────────────── Logging [OPTIONAL] ────────────────
# Options: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
